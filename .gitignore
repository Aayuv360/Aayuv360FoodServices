node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables - Only commit .env for development
.env.local
# Note: .env.development, .env.staging, .env.production are committed for team use
# Real production secrets should be set in deployment dashboard